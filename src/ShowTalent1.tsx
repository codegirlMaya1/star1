import React from 'react';
import Carousel from './Carousel';
import './talentcard1.css'; // Import the CSS file for styling

const ShowTalent1: React.FC = () => {
  return (
    <div className="show-talent-container">
      <Carousel />
    </div>
  );
};

export default ShowTalent1;